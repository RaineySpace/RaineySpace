# UI 规范

整站保留现有页面、内容顺序和组件布局。风格采用中性灰、系统无衬线、克制字号；不以此前生成稿的大标题作为依据。

## 字体与层级

系统字体：system-ui, -apple-system, BlinkMacSystemFont, Segoe UI, PingFang SC, Microsoft YaHei, sans-serif。代码使用系统等宽字体。

| 区域 | 字号 | 字重 | 行高 |
|---|---|---|---|
| 站名、页面标题、文章标题、区块标题 | 16px | 600 | 1.6 |
| Markdown 正文 | 16px | 400 | 1.8 |
| 首页介绍、摘要、页面描述 | 14px | 400 | 1.75 |
| 项目名称 | 14px | 600 | 1.6 |
| 项目描述、日期、标签、目录、辅助链接 | 12px | 400–500 | 1.6 |
| 代码 | 14px | 400 | 1.7 |

Markdown H1–H3 为 16px/600，H4–H6 为 14px/600。普通粗体为 600，无高亮背景或内外边距。桌面与移动端字号相同，不使用超大标题或 font-black。

## 颜色

| 语义 | 浅色 | 深色 |
|---|---|---|
| 页面 | #F9F9F9 | #151618 |
| 卡片 | #FFFFFF | #1D1F22 |
| 柔和表面 | #EFEFF0 | #25272B |
| 标题、链接 | #151618 | #ECEDEF |
| 正文 | #303238 | #D5D7DC |
| 描述 | #60646F | #A8ADB7 |
| 辅助信息 | #727680 | #989EAA |
| 边框 | #DEDFE2 | #35383E |

主题跟随系统。所有页面与 Markdown 使用 globals.css 中同一套语义变量。照片、品牌图标保留原色；代码保留功能性语法着色。灯箱作为观图场景始终使用深色表面。

## 材质与交互

小控件圆角 6px，照片与代码块 8px，介绍框、项目卡片、封面 12px。普通内容无阴影，项目卡片细边框，叠放照片轻阴影。头像及圆形按钮保持圆形。

Markdown 文字链接为中性色、细下划线、单个 ↗；图片链接无箭头。删除线始终清晰，无模糊或背景。普通控件使用 200ms 颜色过渡，无站名和头像放大；保留摄影抬升、灯箱及减少动态效果支持。

## 布局与间距

保留 max-w-2xl 容器和移动端左右 20px 留白。页面上下留白为桌面 48px、移动端 32px；站头到正文为 40px/32px。首页区块间距 40px，区块标题到内容 16px；文章标题、日期、摘要间距 8px，文章条目间距 32px。介绍框内边距 16px，项目／友链卡片间距 12px。摄影条尺寸、重叠、旋转及网格列数不变。

Markdown 段落间距 16px；H1–H3 上方 32px、H4–H6 上方 24px，标题下方 12px；图片、引用、表格、代码块上下 24px；列表项间距 6px。长代码与宽表格仅局部横向滚动。

## 验收与边界

检查 375/768/1280px、浅深主题、Markdown 元素及筛选、目录、灯箱、焦点恢复、Live Photo。执行 pnpm validate:content 与 pnpm build，从 out 验证。保持静态导出、接口、内容模型、排序和过滤语义，不修改 README.md，不自动提交或部署。

## 区块标题行

首页文章、摄影、项目共用 HomeSection 标题行：整行链接到对应列表页，左侧标题 14px/400、颜色 #858892、行高 1.55、细下划线，中间说明 12px/400 使用描述色，右侧 ↗ 使用辅助色。元素间距 16px，箭头靠右；窄屏说明可换行，标题和箭头不压缩。保留 h2 语义与可见键盘焦点，不再单独显示“全部”文字链接。页面主标题和图集标题保持原有职责。

区块标题统一使用 `.section-title`：margin 0、颜色 #858892、14px、normal、400、行高 1.55、字距 0、lowercase；此规则覆盖上表中的区块标题定义，页面和文章主标题不受影响。

文章列表标题与详情页文章主标题使用 16px/400（常规字重），覆盖表中的文章标题字重；Markdown 正文小标题和普通粗体维持原规范。

文章列表顶部筛选标签使用左右 8px、上下 4px 内边距和 28px 高度；折叠容器保持相同高度，字号仍为 12px。

文章、摄影、项目列表页的页头底部间距统一为 12px（margin-bottom），用于衔接列表内容或筛选标签。

站点品牌例外：站名使用系统无衬线字体 600，保留 16px 字号及中性文字色；头像保留 32px 尺寸。站名和头像恢复原有非当前页 hover 放大至 1.2 的行为，过渡 200ms；减少动态效果设置下禁用缩放。此规则替代上文取消品牌缩放的约定。

项目卡片更新为轻量列表行：默认透明、无边框和阴影，hover/键盘焦点/按下时显示柔和表面色；12px 圆角和内边距，48px 图标完整显示，名称 14px/400、描述 12px，不展示日期。桌面图标间距 20px，移动端 12px；保留原有外链及底层日期数据。友链卡片共用同一套基底、样式和列表交互，项目 `cover` 与友链 `icon` 分别作为图标来源；缺少图标或加载失败时显示中性名称首字。此规则替代前述项目卡片白底细边框规范。

项目行描述限制为一行，超出可用宽度使用省略号，不撑开图标。

Markdown 文字链接使用整段行内链接的细底边作为下划线，颜色为 var(--border)，与区块标题浅色下划线一致，覆盖文字、箭头间距及箭头宽度；换行时逐行绘制，保留箭头动效，图片链接不绘制底边。

Markdown 文字链接 hover 和 focus-visible 时，下划线从 var(--border) 过渡到 var(--title)，过渡时间 200ms，与区块标题的悬停颜色一致。

区块标题行的箭头在整行 hover / focus-visible 时，使用与 Markdown 箭头相同的 translate(0.1em, -0.1em) 和 200ms ease 过渡；仅视觉位移，不改变右对齐和布局。减少动态效果设置下禁用该动效。

项目卡片左右使用 -12px margin，抵消 12px 内边距，使 logo 与区块标题左侧对齐；悬停背景向两侧各延伸 12px，内部内容仍保持原有对齐。

文章详情主标题使用 16px/600，文章列表标题仍为 16px/400。详情简介使用与首页介绍一致的柔和背景 var(--surface-muted)、12px 圆角和 16px 内边距，保留 14px 字号，随系统适配深色模式。

文章详情头部顺序：标题、日期/标签、简介卡片、封面，之后为正文。封面上方间距 24px；无简介时仍位于现有头部信息之后，无封面不补造图片。

Markdown 详情页（/[slug]）站头改为浅色“← 返回”，14px，hover/聚焦加深；首页和集合列表页保留站名和头像。优先按 Navigation API 判断是否有上一条历史记录，有则后退，无则 replace 到首页；不支持该 API 的浏览器回退使用 history.length 判断。

宽屏（≥1280px）Markdown 详情页隐藏顶部站头，在正文左侧固定 aside（宽 176px，right: calc(50% + 24rem)，top: 48px），最上方为共享返回按钮，间隔 24px 后显示目录；无目录仍显示返回。窄屏隐藏 aside，保留顶部返回。返回逻辑由 BackButton 共用。

左侧目录容器及条目均无左边框、无左内边距，一级目录与返回按钮左边缘对齐；H3 仍保留 12px 层级缩进，当前项仅通过颜色和字重区分。

返回入口下方间距统一为 24px：窄屏顶部返回站头与正文之间为 24px，宽屏侧栏返回与目录之间为 24px；首页及列表页品牌站头间距不变。

文章详情 article-header 使用纵向 flex 和 8px gap，直接子元素 margin 统一为 0，标题、元信息、简介、封面不再各自添加间距；覆盖此前封面上方 24px 的规则。头部与正文之间的外部间距保持不变。

返回以去掉 hash 的 URL 作为页面身份：一次跨过当前文章所有锚点历史，找到最近的不同页面；没有不同页面时 replace 到首页。支持 Navigation API 时直接计算历史偏移，不支持时逐条后退并跳过同页锚点。

## 阅读设置

入口是一个 `Aa` 文字按钮：32×32、14px/500、辅助色，默认无边框无背景，hover / 聚焦 / 展开时文字变标题色并出现 8px 圆角的柔和表面底。首页与列表页放在站头右侧、头像左边；文章详情页窄屏放在「← 返回」行右侧，宽屏放在左侧栏顶部「← 返回」行右侧，与目录同属阅读辅助区。文案统一为「阅读设置」「文字大小」「行间距」，不使用「大字模式」「适老化」等标签化说法。

面板内容桌面与手机一致：标题「阅读设置」，三组三档分段选择——文字大小「小 / 默认 / 大」、行间距「紧 / 默认 / 宽」、外观「系统 / 浅色 / 深色」。当前档位用柔和表面底加 500 字重表示，点击即时生效。≥640px 使用 176px 宽的 Popover（12px 圆角、卡片色、无边框、轻阴影），站头内右对齐向下展开，侧栏内左对齐 `Aa` 向右下展开；Escape、点击外部或路由变化关闭，焦点回到 `Aa`。<640px 使用底部 Sheet，沿用灯箱 Sheet 的拖动关闭手势，但使用站内浅 / 深色表面变量，分段按钮加高到 40px。减少动态效果设置下不做进出动画。

阅读设置与图片参数共用 `app/components/Sheet.tsx`，统一原生 `dialog`、遮罩、拖动手势和焦点管理，仅内容、表面色与尺寸不同。外层覆盖可视视口，面板自身 `fixed` 贴底，让 Safari 工具栏后方跟随面板表面色；不向视口外叠加背景延伸层。打开时面板以 280ms 滑入，单层遮罩同步渐显；点击遮罩、关闭按钮、Escape 和下拉关闭共用 220ms 滑出与遮罩渐隐，动画结束后再移除弹层、恢复入口焦点。图片参数关闭后保留灯箱及其滚动锁，所有弹层退出后恢复页面滚动。快速关闭时从当前动画位置继续退出，路由切换或组件卸载会清理未完成的动画回调。

字号与行高只切换 CSS 变量，不改变布局、间距与容器宽度。字号档位：小 11 / 13 / 15px（代码 13px），默认 12 / 14 / 16px（代码 14px），大 15 / 18 / 20px（代码 16px），分别对应辅助信息、正文摘要、标题与 Markdown 正文。行间距档位：正文（`--lh-body`）紧 1.6 / 默认 1.75 / 宽 1.9，Markdown（`--lh-prose`）紧 1.65 / 默认 1.8 / 宽 2.0。默认档不在 `<html>` 上写属性，其余档位写 `data-text-size` / `data-leading`；设置以 JSON 存于 localStorage `reading-settings`，由 `<head>` 内联脚本在首屏同步恢复。

外观默认「系统」，即沿用此前跟随系统的行为；「浅色 / 深色」为手动固定。首屏脚本把偏好解析成 `html[data-theme="light|dark"]`，浅深两套语义变量、摄影条边框阴影与代码高亮主题都只依赖该属性，`prefers-color-scheme` 媒体查询仅作为无脚本时的回退。偏好为「系统」时监听系统配色变化并实时更新属性。灯箱仍固定为深色观图表面，不受外观设置影响。

## 首页页脚

首页项目区块之后增加页脚「关于我 / 朋友们」，分别链接 `/about/` 与 `/friends/`。左对齐正文，12px 辅助色文字，上方留白 40px；无额外说明、图标、边框或版权文案。首页不再单独展示友链区块。

## Markdown 数据卡片

正文里的项目卡和友链卡与首页／项目页共用卡片 HTML 与样式。卡片区域隔离正文图片、段落和链接装饰：图标不进入灯箱，名称不加入目录，链接不绘制 Markdown 下划线和箭头。行内引用保持普通名称链接。
